[package]
name = "amoeba"
version = "0.1.0"
edition = "2024"
description = "Edge-aware, scale-to-zero L7 application and compute gateway"
license = "MIT"
repository = "https://github.com/hyperjump888/Amoeba"
readme = "README.md"

[[bin]]
name = "amoeba"
path = "src/main.rs"

[dependencies]
axum = { version = "0.7", features = ["macros"] }
tokio = { version = "1.38", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
arc-swap = "1.7"
notify = "6.1"
jsonwebtoken = "9.3"
reqwest = { version = "0.12", features = ["json"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
tower-http = { version = "0.5", features = ["trace"] }
argon2 = "0.5"
rand_core = { version = "0.6", features = ["getrandom"] }

[dev-dependencies]
tower = { version = "0.5", features = ["util"] }
